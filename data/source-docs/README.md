# Source documents (LOCAL-ONLY — not published)

Drop the real source documents here. **Everything in this folder except this README is git-ignored**, so confidential company material stays off the public repo.

Expected for now:
- **Policy rules** (`.docx`) → parsed into `docs/llm-governance.md` (rules abstracted; verbatim confidential text is **not** copied into the public doc).
- **Hydrocracker unit** details (`.docx`/`.pdf`) → used as the real RAG corpus and to re-center the demo's sample content around the hydrocracker unit.

> If a document is confirmed non-confidential and safe to publish, it can be moved into a tracked folder deliberately — but the default is local-only.
